"""
auth/
=====
Phase 10: Role-based authentication module for EngageLens.

Exports:
    auth_manager   — session validation, login/logout, role guards, timeout
    user_operations — CRUD for users collection + audit_log writes
    seed_admin     — standalone bootstrap script (run directly, not imported)
"""
